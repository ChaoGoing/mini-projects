// 打印键盘布局
console.log(
    (_ =>
        // 将字符串转换为数组并映射每个字符
        [..."`1234567890-=~~QWERTYUIOP[]\\~ASDFGHJKL;'~~ZXCVBNM,./~"].map(x =>
            // 初始化变量
            (o += `/${b = '_'.repeat(w = x < y ? 2 : ' 667699'[x = ["BS", "TAB", "CAPS", "ENTER"][p++] || 'SHIFT', p])}\\|`,
                // 构建键盘布局的每一行
                m += y + (x + '    ').slice(0, w) + y + y,
                n += y + b + y + y,
                l += ' __' + b)[73] &&
            // 将行添加到结果数组
            (k.push(l, m, n, o),
                // 重置变量
                l = '',
                m = n = o = y),
            // 初始化变量
            m = n = o = y = '|',
            p = l = k = []) &&
        // 将结果数组连接成字符串
        k.join`
    `)()
)
